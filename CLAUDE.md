# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

See also: `../CLAUDE.md` for full workspace context.

## Conventions

- All comments and user-facing strings must be in **Japanese**
- TypeScript strict mode — avoid `any`
- Tailwind CSS only for styling (no CSS modules, no inline styles)
